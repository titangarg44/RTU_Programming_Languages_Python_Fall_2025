# Flight Schedule Parser & Query Tool

This is a 2-file implementation of the flight schedule parser.

## 📂 Files
- `flight_parser.py` — main CLI program
- `modules.py` — parsing, validation, and query logic
- `data/db.csv` — sample flight database (raw CSV)
- `data/query.json` — sample query file
